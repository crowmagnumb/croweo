{
    "name": "croweo-node",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/croweo-node/src",
    "projectType": "library",
    "prefix": "crowmagnumb",
    "targets": {
        "build": {
            "executor": "@nx/js:tsc",
            "outputs": ["{workspaceRoot}/dist/libs/croweo-node"],
            "options": {
                "main": "{projectRoot}/src/index.ts",
                "outputPath": "dist/{projectRoot}",
                "tsConfig": "libs/croweo-node/tsconfig.lib.json"
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "libs/croweo-node/jest.config.ts"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "options": {
                "lintFilePatterns": ["libs/croweo-node/src/**/*.ts"]
            }
        }
    },
    "tags": []
}
